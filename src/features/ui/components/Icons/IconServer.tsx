import { IconProps, IconServer as IconServerTabler } from '@tabler/icons-react';

interface Props extends IconProps {}

export const IconServer = (props: Props) => {
  return <IconServerTabler {...props} />;
};
